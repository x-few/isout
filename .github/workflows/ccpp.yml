name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: install deps
      run: sudo apt-get install autoconf automake libtool -y
    - name: configure
      run: ./autogen.sh
    - name: make
      run: make
